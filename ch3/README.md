# ch3

目标：
- 用 `pthread` 实现线程池

修改文件：
- `ch3/thread_pool.c`

要做的事：
- 完成 `thread_pool_worker(...)`
- 完成 `thread_pool_submit(...)`
- 完成 `thread_pool_wait(...)`
- 完成 `thread_pool_destroy(...)`

运行：

```bash
./autograde.sh ch3
```
